print("Hello ! Welcome back to c--")
print("updates available today:目前代码行数已达到107行，为保证安全，我们已将代码加密。前往Github 获取源码。")
print("做各种中文搞笑程序、梗程序，来抖音找Web_，制作找QQ：2907872764")

while True: 
    call = "尚未登入"
    break
print("登入状态：",call,".")



for x in range(888):

    a = input("C-->")

    if a == "help":
        print("printf:printf + text > text")
        print("exit:exit > exit to c-- ")
        print("in:in + prompt text + int > in: ")
        print("help:help > get help")
        print("-----------------")
    elif a == "printf":
        text = input("Enter your text:")
        print(text)
        print("-----------------")
    elif a == "exit":
        exit()
    elif a == "in":
        con = input("Enter your input prompt text:")
        b = input(con)
        print(b)
        print("-----------------")
    elif a == "viv":
        print("Web_ C-- [版本 2.10.001]")
        print("开源软件，转到GitHub 上下载源码？")
        print("-----------------")
    elif a == "if":
        sync = input("输入事件类型：")
        sure = input("确实是这个吗？(Y/N)")
        if sure == "Y":
            a = print("Sync 无法比较事件，请重试")
            a
            print("按任意键继续:")
            print("-----------------")
        else:
            print("哦！您取消了操作！")
            print("-----------------")
    elif a == "bi":
        ba = input("输入你要比较的数值（字符串、密钥串等可识别字符）:")
        int(ba)
        print("正在处理 |||||||||||||||100%|||||||||||||||")
        bb = input("输入被比较数值（字符串、密钥串等可识别字符）:")
        int(bb)
        print("正在比较 |||||||||||||||100%|||||||||||||||")
        if ba > bb:
            print("大于！")
            print("-----------------")
        elif ba < bb:
            print("小于！")
            print("-----------------")
        else:
            print("等于或未知")
            print("-----------------")
    elif a == "mone":
        sure2 = input("你确实要进入模拟从模块异常退出模式么？（Y/N）")
        if sure2 == "Y":
            print("Process finished with exit code -1")
            print("出现异常，导致C--异常退出。我们已经报告给Web_，正在上传数据：")
            import time
            print("正在准备上传 |                      |0%")
            time.sleep(5)
            print("上传中.. |||||                 |15%")
            time.sleep(2)
            print("上传成功！速度2Kb/s，太棒了！")
            input("按任意键重新启动C--")
        else:
            print("为安全起见，已停止模拟故障")
            input("按任意键继续:")
    elif a == "send":
        isaip = input("输入目标服务器地址：")
        print("正在等待对方响应...")
        print("|                       |0% 已传输，等待对方响应...")
        import time
        time.sleep(20)
        print("|||||||||||||||||||||||||100% 传输已完成，成功：0，失败：1，对方长时间未作出相应。Err_TimeOut")
        print("你可以做出如下选择：")
        print("1.通知对方尽快做出响应.")
        print("2.通知对方关闭防火墙并尝试重新发送.")
        print("3.检查服务器地址.")
        ch = input("请选择：")
        if ch == "1":
            print("||||||||||||||||||||||||||||||||100% 通知已送达，对方不在线。")
            print("通知传输失败，已驳回通知。")
            input("按任意键退出")
        elif ch == "2":
            print("你需要通过其他方式通知，对方防火墙已打开，我们无法访问对方电脑（PC）")
            input("按任意键退出")
        else:
            print("检查服务器地址，你刚刚输入的是：",isaip,",若不正确，请重新启动发送程序.")
            input("按任意键退出")
    elif a == "kuozhan":
        print("尚未在工作区检查到扩展，请运行扩展。")
        print("小提示：该扩展未经过Web_认证？打开开发者模式请输入od")
        input("按任意键退出：")
    elif a == "od":
        opened = "关"
        print("当前开发者模式开启状态为:", opened, "。")
        odw = input("开起它？（请注意：开发者模式会导致C-- 不稳定或崩溃，为保证C--正常启动，你每次在控制台输入od时开发者模式就会关闭。）(Y/N)")
        if odw == "Y":
            print("开发者模式已开启.")
            input("按任意键进入开发者模式")
        else :
            print("取消操作")
            input("按任意键脱离第三方模块")
    elif a == "oweb":
        import os
        import webbrowser
        https = input("输入你要访问的网址(默认使用Internat Ex 访问)：")
        webbrowser.open(https, new=0, autoraise=True)
        print("已经进入了。（仍未进入？遇到问题？反馈给Web_）")
    elif a == "login":
        import webbrowser
        log = input("即将登入C-- 帐户，按回车键继续")
        webbrowser.open("xiutangh5.cn/p/c89382fc.html", new=0, autoraise=True)
        input("我们打开了一个浏览器选项卡，在那里面登录把！完成后关闭浏览器并按回车键")
        
        if call == None:
            call = "已登录"
            pass
        elif call != None:
            print("出现问题：变量call不等于None,因此程序无法继续。")
            exit()
        else:
            print("出现问题:程序不小心执行了else,因此程序无法继续")
            exit()
        



    
        
    







    else:
        print("未知命令或自定义命令文件损坏或被移动，检查它！Error:NoneCommandOfThis")
        input("Press any key ...")
        print("-----------------")
 